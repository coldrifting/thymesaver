import SwiftUI

/*
extension View {
    func amountEntryAlert(
        placeholder: String,
        onConfirm: @escaping (Amount) -> Void,
        @Binding show: Bool
        @Binding text: String
    ) -> some View {
        self.alert(
            "Select Ingredient Quantity",
            isPresented: $show,
            actions: {
                if (alertType != .delete) {
                    TextField(placeholder, text: $text)
                }
                
                Picker(selection: <#T##Binding<Hashable>#>, content: <#T##() -> View#>, label: <#T##() -> View#>)
                
                Button(alertType.description, role: alertType == .delete ? .destructive : .none, action: {
                    withAnimation {
                        onConfirm(text.trim())
                    }
                })
                Button("Cancel", role: .cancel, action: {
                })
            },
            message: { Text(message) })
    }
}
*/
